---
import KPIItem from "../components/KPIItem.tsx";
const { blok } = Astro.props;
---

<KPIItem
  client:only="react"
  label={blok.label}
  value={blok.value}
  unit={blok.unit}
  description={blok.description}
  cta_text={blok.cta_text}
  cta_url={blok.cta_url}
/>
